Place the three PDFs in this folder, named exactly:

  FSG-v6-2-2025-02.pdf
  Adviser-Profile-v6-1-2023-04.pdf
  AVALONFS-Privacy-Policy-2023-01.pdf

The filenames are referenced from data/site.ts. If you change one, change it
there — not in the pages, which read from that file.

The Adviser Profile is not optional. FSG v6.2 states that page 11 is missing
from downloaded copies, so publishing the annexure separately is what makes the
FSG complete.

Version the filenames and keep old versions when a document is replaced — do
not overwrite. Every version made available must be retained for seven years.
When a URL is retired, add a redirect to `legacyDocuments` in next.config.ts
rather than letting it 404.
